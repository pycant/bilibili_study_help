# 2026-04-12

## B站学习专注助手脚本 Bug 修复 (v1.0.4)

完成了 bilibili-study-focus-assistant.user.js 的三个 Bug 修复：

1. **Bug1 (课程选择无反应)**：修复了 `showConfirmModal()` 中的内联 onclick 模板字符串未求值 + disabled 按钮 addEventListener 不触发的问题，改用 `data-*` 属性 + 事件委托
2. **Bug2 (暗色模式)**：创建 `applyCurrentThemeToModal()` 函数，对所有干预弹窗（确认弹窗、Stage2、单词验证）应用 `bilibili-study-dark-mode` class
3. **Bug3 (全屏模式)**：创建 `getModalContainer()` 函数检测全屏元素，弹窗挂载到全屏元素内部，解决 z-index 覆盖问题

版本号统一为 v1.0.4，已 git commit。

## 文档更新

- **improvement.md**：将已完成的新增需求（1.1填词替换、1.2课程选择返回）移入已完成区域，补充v1.0.4三个bug修复记录
- **SCRIPT_LOGIC.md**：新建脚本运行逻辑详解文档，涵盖14个章节：整体架构、初始化流程、核心状态机、计时机制、白名单机制、配置系统、统计系统、UI系统、主题系统、全屏兼容、SPA路由处理、单词学习系统、存储系统、关键数据流
