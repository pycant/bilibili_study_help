# 2026-04-14

## B站学习专注助手脚本 Bug修复

### v1.0.6: 单词验证弹窗不显示（v1.0.5回归Bug）
- 根因：`renderWordModalContent()` 首次调用时 modal 是空 div，`.bilibili-study-modal-body` 不存在
- 修复：区分首次渲染与后续渲染

### v1.0.7: SPA导航导致干预状态重置
- 根因：`observeSPAChanges` 回调无条件执行 `InterventionController.reset()`，切视频后所有状态归零
- 用户从非白名单视频A切到非白名单视频B，`distractionStartTime` 被清空，干预计时从零开始
- 修复：SPA导航时区分白名单/非白名单视频，非白名单视频保留干预状态只关闭弹窗
